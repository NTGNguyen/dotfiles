[[ $- != *i* ]] && return

function load_zsh_plugins {
  zsh_paths=(
    "$HOME/.oh-my-zsh"
    "/usr/local/share/oh-my-zsh"
    "/usr/share/oh-my-zsh"
  )
  for zsh_path in "${zsh_paths[@]}"; do [[ -d $zsh_path ]] && export ZSH=$zsh_path && break; done
  # Load Plugins
  hyde_plugins=( git zsh-256color zsh-autosuggestions zsh-syntax-highlighting nx-completion )
  plugins+=(
    "${plugins[@]}"
    "${hyde_plugins[@]}"
    mise
    git
    zsh-256color
    zsh-autosuggestions
    zsh-syntax-highlighting
    history-substring-search
    dotenv
    sudo
    fzf-tab
    zsh-wakatime
    gradle-completion
    golang
    kubectl
    nix-zsh-completions
  )
  # Deduplicate plugins
  plugins=("${plugins[@]}")
  plugins=($(printf "%s\n" "${plugins[@]}" | sort -u))

  # Loads om-my-zsh
  [[ -r $ZSH/oh-my-zsh.sh ]] && source $ZSH/oh-my-zsh.sh
}

source ~/zsh/completions.zsh

load_zsh_plugins
fastfetch

#PK10Theme
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

P10k_THEME=${P10k_THEME:-/usr/share/zsh-theme-powerlevel10k/powerlevel10k.zsh-theme}
[[ -r $P10k_THEME ]] && source $P10k_THEME
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh

# Load fzf key bindings (enables Ctrl+R) and auto-completion
if [ -f /usr/share/fzf/key-bindings.$(basename $SHELL) ]; then
  source /usr/share/fzf/key-bindings.$(basename $SHELL)
  source /usr/share/fzf/completion.$(basename $SHELL)
fi


#inti zoxide
eval "$(zoxide init zsh)"

source ~/zsh/aur.zsh
source ~/zsh/hyde_handle_function.zsh
source ~/zsh/history.zsh
source ~/zsh/custom-function.zsh
source ~/zsh/alias.zsh
source ~/zsh/exit.zsh
source ~/zsh/kus_com.zsh

# chpwd hook
chpwd () {
  ls
}

export GPG_TTY=$(tty)

. "$HOME/.cargo/env"            # For sh/bash/zsh/ash/dash/pdksh

if type compdef &>/dev/null; then
  _pnpm_completion () {
    local reply
    local si=$IFS

    IFS=$'\n' reply=($(COMP_CWORD="$((CURRENT-1))" COMP_LINE="$BUFFER" COMP_POINT="$CURSOR" SHELL=zsh pnpm completion-server -- "${words[@]}"))
    IFS=$si

    if [ "$reply" = "__tabtab_complete_files__" ]; then
      _files
    else
      _describe 'values' reply
    fi
  }
  # When called by the Zsh completion system, this will end with
  # "loadautofunc" when initially autoloaded and "shfunc" later on, otherwise,
  # the script was "eval"-ed so use "compdef" to register it with the
  # completion system
  if [[ $zsh_eval_context == *func ]]; then
    _pnpm_completion "$@"
  else
    compdef _pnpm_completion pnpm
  fi
fi
export FZF_DEFAULT_OPTS="--height 40% --layout=reverse --border=rounded --margin=1 --padding=1 --color=hl:#ff5c57,hl+:#ff5c57,info:#afaf87,prompt:#50fa7b,pointer:#ff92df,marker:#ff92df,spinner:#ff92df"
